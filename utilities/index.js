const {
  getEntry,
  getAsset,
  getPageContent,
  getContentfulEntry,
} = require('./cms');

const { getPageId } = require('./getPageId');

module.exports = {
  getEntry,
  getAsset,
  getPageContent,
  getPageId,
  getContentfulEntry,
};